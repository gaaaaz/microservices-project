spring:
  application:
    name: service-gateway # Nombre de la aplicacion y nuestro archivo de configuracion 
  config:
    import: optional:configserver:http://localhost:8081 
  cloud:
    config:
      uri: http://locahost:8081 # Ruta de service-config (Configuracion de Spring Cloud)
      username: root
      password: s3cr3t
